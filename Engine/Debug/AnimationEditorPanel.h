#pragma once

#include <string>

namespace mygame
{
    /**
     * \brief Draw the sprite Animation Editor window if toggled on.
     * \param open Reference to the visibility flag; updated when the window is closed.
     */
    void DrawAnimationEditor(bool& open);
}
